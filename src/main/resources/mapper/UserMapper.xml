<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.bishe.question.mapper.UserMapper">

    <resultMap id="userMap" type="org.bishe.question.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="status" column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createdTime" column="created_time"/>
        <result property="updatedTime" column="updated_time"/>
    </resultMap>

    <select id="findById" resultMap="userMap">SELECT * FROM users WHERE id = #{id}</select>
    <select id="findAll" resultMap="userMap">SELECT * FROM users ORDER BY id</select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, email, phone, nickname, avatar, status, last_login_time, created_time, updated_time)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{nickname}, #{avatar}, #{status}, #{lastLoginTime}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE users <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="status != null">status = #{status},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            updated_time = NOW()
        </set> WHERE id = #{id}
    </update>

    <delete id="deleteById">DELETE FROM users WHERE id = #{id}</delete>
    <select id="count" resultType="long">SELECT COUNT(*) FROM users</select>

    <select id="findByUsername" resultMap="userMap">SELECT * FROM users WHERE username = #{username}</select>
    <select id="findByEmail" resultMap="userMap">SELECT * FROM users WHERE email = #{email}</select>
    <select id="findByUsernameOrEmail" resultMap="userMap">
        SELECT * FROM users WHERE username = #{username} OR email = #{email}
    </select>
    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE username = #{username}
    </select>
    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email}
    </select>
    <select id="findByStatus" resultMap="userMap">SELECT * FROM users WHERE status = #{status}</select>
    <select id="countByStatus" resultType="long">SELECT COUNT(*) FROM users WHERE status = #{status}</select>
    <select id="findByKeyword" resultMap="userMap">
        SELECT * FROM users
        WHERE username LIKE CONCAT('%',#{keyword},'%')
           OR nickname LIKE CONCAT('%',#{keyword},'%')
           OR email LIKE CONCAT('%',#{keyword},'%')
    </select>
    <select id="findRecentlyActiveUsers" resultMap="userMap">
        SELECT * FROM users WHERE last_login_time >= #{since} ORDER BY last_login_time DESC
    </select>
    <select id="findNeverLoggedInUsers" resultMap="userMap">
        SELECT * FROM users WHERE last_login_time IS NULL
    </select>
    <select id="findByCreatedTimeBetween" resultMap="userMap">
        SELECT * FROM users WHERE created_time BETWEEN #{startTime} AND #{endTime}
    </select>
</mapper>
